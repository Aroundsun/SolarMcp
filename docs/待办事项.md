# SolarMcp 待办事项

> 汇总后续可迭代项，按优先级排列。完成一项可在 `[ ]` 打勾为 `[x]`。  
> 最后更新：2026-06-23（Multi-Reactor + ThreadPool 已落地）

相关文档：[插件安全.md](./插件安全.md)、[多Reactor架构.md](./多Reactor架构.md)、[系统设计.md](./系统设计.md)

---

## 已完成（备忘）

- [x] 纯 C 插件 ABI v2 + CallbackTool
- [x] 插件配置解耦（一插件一子目录 + 自有 yaml）
- [x] Main Reactor + Worker Reactor（`EventLoopThreadPool`）
- [x] 业务 ThreadPool 异步 `tools/call` / `plugins/reload`
- [x] `ToolManager` / `PluginManager` 读写锁
- [x] 压测脚本与存档（`scripts/load_test.py`、`scripts/benchmark_advanced.py`）

---

## P0 — 生产硬化（建议优先）

### 1. 插件目录治理

- [ ] Core 配置增加 `plugins.allowlist: [shell, example]`，只加载白名单子目录
- [ ] 扫描时忽略 `plugins/lib/` 等 legacy 目录
- [ ] 部署文档 / 脚本：删除残留 `plugins/lib/*.so`
- [ ] 生产配置模板（`config.prod.yaml.example`）：强随机 token、`allow_reload: false`

**参考**：`src/plugin/plugin_manager.cpp` → `scanPluginEntries()`

**验收**：非 allowlist 目录下的 `.so` 不被加载；`plugins/lib/` 不再被统计。

---

### 2. 配置与运维默认值

- [ ] 根据本机压测，将 `reactor.io_threads` 从 `4` 调整为 `2`（p99 更优）或文档说明按 CPU 扫参
- [ ] README 增加「生产检查清单」链接 → [插件安全.md §6](./插件安全.md#6-生产环境检查清单)
- [ ] 启动日志打印 Reactor 模式、IO 线程数、ThreadPool 大小（部分已有，可核对完整性）

**参考**：[`docs/benchmarks/2026-06-23-advanced-benchmark.md`](./benchmarks/2026-06-23-advanced-benchmark.md)

---

### 3. 热重载并发保护

- [ ] `plugins/reload` 与进行中的 `tools/call` 互斥（全局读写锁或 in-flight 计数）
- [ ] 重载期间新 `tools/call` 返回明确错误码（如 `-32006 Reload in progress`）
- [ ] 集成测试：并发 reload + call 不崩溃、不 UAF

**参考**：`docs/插件安全.md` §3、`PluginManager` + `ToolManager`

---

## P1 — 功能完善

### 4. 连接 idle 超时（时间轮第一用途）

- [ ] 修复 `EventLoop` 时间 tick：按真实 elapsed 补 tick，或 `poll` 超时取 `min(下次定时, max_wait)`
- [ ] `main.cpp` 创建 `TimerWheel`，挂到各 Worker Loop（或每连接在其所属 Loop 注册）
- [ ] `TcpConnection`：有读写则 reset；超时 `shutdown()`
- [ ] 配置：`server.idle_timeout_sec`（默认 0 = 禁用）
- [ ] 单元测试 `tests/test_timer_wheel.cpp`

**暂不做**：给每个 `tools/call` 包 TimerWheel（与 shell 内置 poll 超时重复，且 ThreadPool 难中断）

**参考**：`include/mcp/timer/timer_wheel.h`、`src/reactor/event_loop.cpp`

---

### 5. 插件完整性校验

- [ ] 构建时为每个 `.so` 生成 sidecar（`.sha256` 或 `manifest.json`）
- [ ] `loadPlugin` / `validatePlugin` 前比对哈希，失败拒绝加载
- [ ] 可选：Ed25519 签名（P2 扩展）

**参考**：`docs/插件安全.md` §4 P1

---

### 6. 工具级 ACL

- [ ] 策略文件（如 `config/policy.yaml`）：token 或角色 → 允许的工具列表
- [ ] `tools/call` 分发前校验（可在 `Dispatcher` handler 或 `TcpServer` 层）
- [ ] 测试：受限 token 只能调用 `read_file`，不能调用 `shell`

---

### 7. 文档同步

- [ ] `docs/系统设计.md`：更新为 Multi-Reactor + ThreadPool 现状（仍有 P0 单 Reactor 描述）
- [ ] `docs/时序图.mermaid`：补充异步 `tools/call` 路径（ThreadPool → sendInLoop）
- [ ] `docs/类图.mermaid`：增加 `EventLoopThreadPool`、`EventLoopThread`
- [ ] `plugins/README.md`：确认无 `plugins/lib` 旧说明

---

## P2 — 增强与扩展

### 8. shell 子进程隔离

- [ ] 高危工具独立 helper 进程，IPC 传 JSON（或 seccomp / Landlock）
- [ ] 主进程即使被插件漏洞利用，也不直接 fork 用户命令

**参考**：`docs/插件安全.md` §4 P2

---

### 9. 资源配额

- [ ] 全局：单工具 CPU 时间、内存、输出上限（除 shell 已有 timeout/output）
- [ ] ThreadPool 队列深度监控 / metrics 暴露

---

### 10. Transport 扩展

- [ ] Unix Domain Socket transport（`config.yaml` → `server.transport: unix`）
- [ ] `Transport` 抽象 + `UnixTransport` 实现

**参考**：`docs/系统设计.md` §1.3.12

---

### 11. 可观测性

- [ ] 简单 metrics 端点或日志统计：QPS、连接数、ThreadPool 队列长度、各工具 call 次数
- [ ] 可选：对接 Prometheus

---

### 12. Context 协作式取消

- [ ] `Context.cancelled` / `timeout_ms` 在长时间工具中轮询
- [ ] `read_file` 大文件分块读时可中断
- [ ] 与 TimerWheel 或独立 watchdog 配合（低优先级）

---

## 测试与压测 backlog

- [ ] CI：Release 构建 + `ctest` + 可选 smoke `client_test.py`
- [ ] 压测纳入 CI nightly（可选，VM 上跑 fixed 5000 套件）
- [ ] 混合负载回归：`benchmark_advanced.py` 作为 release 前手动检查项
- [ ] 对比 `reactor.mode: single` vs `multi` 的自动化报告（已有脚本，可固化 baseline JSON）

**脚本**：

```bash
python3 scripts/load_test.py --suite
python3 scripts/benchmark_advanced.py
```

---

## 代码质量 / 技术债

- [ ] `TimerWheel::cancelTimer` O(n) 扫描 → id → slot 索引表（若启用时间轮再做）
- [ ] `EventLoopThreadPool` / `TcpServer` 集成测试（多 Worker 下并发 list + call）
- [ ] `config.bench.yaml` 加入 `.gitignore`（`benchmark_advanced.py` 生成）
- [ ] 未提交改动整理 git commit（Multi-Reactor、压测、event_loop Werror 修复等）

---

## 建议实施顺序（总览）

```
P0: allowlist + legacy 清理 → 热重载锁 → 生产配置模板
P1: idle 超时（时间轮）→ 哈希校验 → ACL → 文档同步
P2: shell 隔离 → metrics → Unix transport
```

---

## 单项估时（供排期）

| 项 | 估时 |
|----|------|
| plugins.allowlist + 忽略 lib | 0.5–1 天 |
| 热重载并发锁 | 1 天 |
| 连接 idle 超时 + 修 tick | 1–2 天 |
| .so 哈希校验 | 1–2 天 |
| 工具级 ACL | 2–3 天 |
| 文档同步 | 0.5 天 |
| shell 子进程隔离 | 1–2 周 |

---

## 相关文件速查

| 领域 | 路径 |
|------|------|
| 插件加载 | `src/plugin/plugin_manager.cpp` |
| 认证 | `src/server/tcp_server.cpp` |
| 异步分发 | `src/server/tcp_server.cpp` → `dispatchMessage` |
| MCP 方法注册 | `app/main.cpp` → `registerMcpMethods` |
| 多 Reactor | `src/reactor/event_loop_thread_pool.cpp` |
| 业务线程池 | `src/thread/thread_pool.cpp` |
| 时间轮 | `src/timer/timer_wheel.cpp` |
| 主配置 | `config.yaml` |
| 压测 | `scripts/load_test.py`、`scripts/benchmark_advanced.py` |
